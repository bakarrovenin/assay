---
import Logo from './Logo.astro';

// Eight repetitions: four fill a wide viewport, and the track is duplicated
// so the -50% translate loops seamlessly.
const units = Array.from({ length: 8 });
---

<div class="marquee raised border-y border-line py-7" aria-hidden="true">
  <div class="marquee-track">
    {
      units.map(() => (
        <span class="marquee-unit">
          <Logo size={17} wordmark={false} />
          <span class="marquee-text">Nullius in verba</span>
        </span>
      ))
    }
  </div>
</div>

<p class="disc mt-5 text-center">
  Take nobody&rsquo;s word for it. Royal Society, 1660.
</p>

<style>
  .marquee {
    overflow: hidden;
    user-select: none;
    /* Feather the ends so the loop seam never lands hard against the edge. */
    mask-image: linear-gradient(
      to right,
      transparent,
      #000 7%,
      #000 93%,
      transparent
    );
  }

  .marquee-track {
    display: flex;
    width: max-content;
    align-items: center;
    animation: marquee-scroll 40s linear infinite;
  }

  .marquee:hover .marquee-track {
    animation-play-state: paused;
  }

  .marquee-unit {
    display: inline-flex;
    align-items: center;
    gap: 26px;
    padding-inline: 26px;
    color: var(--fg);
  }

  .marquee-text {
    font-family: var(--font-mono);
    font-size: var(--t-h3);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.3em;
    white-space: nowrap;
  }

  @keyframes marquee-scroll {
    from {
      transform: translateX(0);
    }
    to {
      transform: translateX(-50%);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    :global(html:not([data-motion="on"])) .marquee-track {
      animation: none;
      justify-content: center;
      width: 100%;
    }
    /* Static state shows one instance, not a clipped row of eight. */
    :global(html:not([data-motion="on"])) .marquee-unit:not(:first-child) {
      display: none;
    }
  }
</style>
